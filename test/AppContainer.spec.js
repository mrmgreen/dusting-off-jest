import React from 'react';
import { shallow, mount } from 'enzyme';
import AppContainer from '../../src/container/AppContainer';
import { expect } from 'chai';

describe("<AppContainer />", () => {
  it("if pathname is a recognised pathname it will be the title prop of Programme component", () => {

  });
  it("if pathname is a recognised pathname it will pass it's own handleClick prop to Programme component", () => {

  });
});